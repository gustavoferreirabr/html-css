//    indice:   0   1  2  3
const numeros = [1, 2, 3, 4]
const [primeiroIndice, segundoIndice] = numeros; // Isso daqui apenas funciona com números, foi feito o teste.

console.log(primeiroIndice, segundoIndice);